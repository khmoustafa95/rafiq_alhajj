# Dart defines (moved)

Configuration files now live under **`config/dart-defines/`**.

See [config/dart-defines/README.md](config/dart-defines/README.md) for the full platform × environment matrix.

Quick start:

```powershell
npm run config:bootstrap
```

Legacy root files (`dart_defines*.local.json`) are still supported as fallbacks but are deprecated.
